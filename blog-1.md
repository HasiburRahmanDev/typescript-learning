TypeScript help us to make JavaScript safer by enforcing types.
Both Any and unknown type takes any type of data but Unknown is safer because it forces you to validate before using it while Any type doesn't do it.
